server:
  port: 56020
  servlet:
    context-path: /application1
spring:
  application:
    name: application1
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace:
        cluster-name: DEFAULT

      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        namespace:
        group:
